import ExpertProfilePage from '@/components/ExpertProfilePage';

export default function ExpertProfile({ params }: { params: { id: string } }) {
  // TODO: Fetch expert data from API using params.id
  return <ExpertProfilePage />;
}
